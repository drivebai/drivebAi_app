# Baked into the production bundle by `vite build`.
# This file IS committed — it contains only the public backend URL,
# no secrets. Do not put admin credentials or API keys here.
VITE_API_BASE_URL=https://drivebai-api-team.fly.dev
